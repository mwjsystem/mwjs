import { GcdinputDirective } from './gcdinput.directive';

describe('GcdinputDirective', () => {
  it('should create an instance', () => {
    const directive = new GcdinputDirective();
    expect(directive).toBeTruthy();
  });
});
